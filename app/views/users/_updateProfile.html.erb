<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_area :first_name, required: true %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_area :last_name, required: true %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email, required: true %>
  </div>
  <div class="field">
    <%= f.label :bio %><br>
    <%= f.text_area :bio, required: false %>
  </div>

  <%= f.submit :Submit, class: 'btn btn-success' %>
<% end %>
